# Init the .work strategy

## Folder structure

- Create the following folder structure in the repository:

```markdown
.work/
.work/agent/
.work/agent/notes/
```

## Issue Management

- Initialize the issue tracking system:

```bash
uv run issues init
```

- All issue management is handled via `uv run issues` commands
- No markdown files are used for tracking issues



